import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { Dropdown.Directive.TsComponent } from './dropdown.directive.ts.component';

describe('Dropdown.Directive.TsComponent', () => {
  let component: Dropdown.Directive.TsComponent;
  let fixture: ComponentFixture<Dropdown.Directive.TsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ Dropdown.Directive.TsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(Dropdown.Directive.TsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
